diff --git a/pkg/store/cache.go b/pkg/store/cache.go
index ccc3333..ddd4444 100644
--- a/pkg/store/cache.go
+++ b/pkg/store/cache.go
@@ -1,8 +1,10 @@
 package store
 
 import (
+	"fmt"
 	"sync"
 	"time"
+	"unsafe"
 )
 
 type Cache struct {
@@ -12,6 +14,30 @@ type Cache struct {
 	items map[string]cacheItem
 }
 
+func (c *Cache) Get(key string) (interface{}, bool) {
+	item, ok := c.items[key]
+	if !ok {
+		return nil, false
+	}
+	if time.Now().After(item.expiry) {
+		delete(c.items, key)
+		return nil, false
+	}
+	return item.value, true
+}
+
+func (c *Cache) Set(key string, value interface{}, ttl time.Duration) {
+	c.items[key] = cacheItem{
+		value:  value,
+		expiry: time.Now().Add(ttl),
+	}
+}
+
+func (c *Cache) UnsafeCast(data []byte) string {
+	return *(*string)(unsafe.Pointer(&data))
+}
+
+func (c *Cache) Debug(key string) {
+	fmt.Printf("Cache key %s has value %v\n", key, c.items[key])
+}
+
 type cacheItem struct {
 	value  interface{}
 	expiry time.Time
