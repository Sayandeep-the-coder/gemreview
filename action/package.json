{
  "name": "gemreview-action",
  "version": "1.2.0",
  "private": true,
  "description": "Action entry point for gemreview GitHub Action",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsup src/index.ts --format cjs --outDir dist --no-splitting --minify"
  },
  "dependencies": {
    "@actions/core": "^1.10.1"
  }
}
